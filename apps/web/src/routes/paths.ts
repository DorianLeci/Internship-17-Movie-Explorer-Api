export enum AppPaths {
  REGISTER = '/register',
  LOGIN = '/login',
  HOME = '/',
  MOVIES = '/movies',
  MOVIE_DETAIL = '/movies/:id',
  FAVORITES = '/favorites',
  NOT_FOUND = '/not-found',
  NON_EXSTING = '*',
}
