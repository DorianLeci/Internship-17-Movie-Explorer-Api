export enum AppPaths {
  REGISTER = '/register',
  LOGIN = '/login',
  HOME = '/',
  MOVIES = '/movies',
  ADMIN_MOVIES = '/admin/movies',
  MOVIE_DETAIL = '/movies/:id',
  FAVORITES = '/favorites',
  NOT_FOUND = '/not-found',
  FORBIDDEN = '/forbidden',
  NON_EXSTING = '*',
}
